# Database Configuration for Railway
# Railway provides MYSQLHOST, MYSQLPORT, MYSQLDATABASE, MYSQLUSER, and MYSQLPASSWORD
spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:ecommerce_db}?allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:M@n@vsinh_1709ms}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Custom JWT Secret
# Ensure JWT_SECRET is added in the Railway 'Variables' tab
app.jwt.secret=${JWT_SECRET:MTYyN2M4YjA1OTY4YTg1YTM2ZDFiYjA3ZGE1Y2M0ZTM_DEVELOPMENT_ONLY}
app.jwt.expiration=86400000

# Server Configuration for Cloud Networking
server.port=${PORT:8080}
server.address=0.0.0.0